import { InputDateDirective } from './input-date.directive';

describe('InputDateDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDateDirective();
    expect(directive).toBeTruthy();
  });
});
